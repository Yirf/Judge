name: judge
version: '0.8'
main: me.yirf.judge.Judge
api-version: '1.20'
authors: [ Yirf, Xtevv ]
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    ViaVersion:
      load: BEFORE
      required: true
    Citizens:
      load: BEFORE
      required: false

commands:
  reloadjudge:
    description: reloads config